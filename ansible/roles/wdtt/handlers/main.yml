---
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart wdtt-server
  ansible.builtin.systemd:
    name: wdtt-server.service
    state: restarted

- name: reload nftables
  ansible.builtin.command: nft -f /etc/nftables.conf
