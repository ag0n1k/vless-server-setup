---
- name: reload nftables
  ansible.builtin.command: nft -f /etc/nftables.conf

- name: restart tproxy-routing
  ansible.builtin.systemd:
    name: "{{ tproxy_routing_unit }}"
    state: restarted
    daemon_reload: true
